# Byte-compiled / build artifacts
__pycache__/
*.py[cod]
*$py.class

# Datasets (downloaded/mounted locally, not part of the repo)
data/

# Model checkpoints (large binaries, machine-specific paths)
model_checkpoints/
checkpoints/
*.pth
*.pt

# Experiment outputs generated by running the pipeline
*.csv
*.json
*_loss_curves.png
*_es_trajectory.npy
*_es_meta.json
*_tsne*.png
mia_loss_histogram_*.jpg

# Local scratch/working notes for this migration (not part of the published repo)
migration_notes/

# Editor / OS
.DS_Store
.vscode/
.idea/

# Python environments
venv/
.venv/
env/
